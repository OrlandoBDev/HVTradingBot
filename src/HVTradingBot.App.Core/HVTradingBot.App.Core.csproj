<Project Sdk="Microsoft.NET.Sdk">

  <!-- Logic of the macOS app (apps/HVTradingBot.App). No MAUI dependency, so it builds and is tested on Linux. -->

  <ItemGroup>
    <InternalsVisibleTo Include="HVTradingBot.App.Tests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.12" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
